# real secrets — never commit
.env
.env.*
!.env.example

# supabase CLI local state
.supabase/
supabase/.temp/
supabase/.branches/

# local test harness scratch
tests/local/*.log

node_modules/
.DS_Store

# لوحة التحكم: مفاتيح النشر الحقيقية
dashboard/config.js

# Brand icons land here only when the dashboard test suite runs against the
# local mock storage. Real icons live in the Supabase product-media bucket.
assets/product-media/products/icons/
