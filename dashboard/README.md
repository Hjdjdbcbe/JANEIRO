# لوحة Janeiro

**تُنشر وحدها، على نطاق منفصل — لا داخل المتجر.**

هذا المجلد تطبيق قائم بذاته: ملف واحد بلا اعتماديات ولا بناء. لا يشترك
مع المتجر في أي شيء غير قاعدة بيانات Supabase.

## لماذا منفصلة

وضعها على `janeiro.com/admin` يجعلها جزءاً من نشر المتجر: أي زائر يجرّب
العنوان يجدها، وأي خطأ في إعداد النشر يكشفها، وكل إصدار للمتجر يعيد
نشرها معه.

الأمان هنا لا يعتمد على إخفاء العنوان — `admin_update_order_status`
و`admin_dashboard_stats` تفحصان `is_admin()` في الخادم، فحتى من وجد
الصفحة لا يحصل على شيء. لكن **الفصل يقلّل السطح**: صفحة أقل على نطاقك
العام، ونشر لا يخلط أداة داخلية بواجهة زبائن.

## النشر

انشر **هذا المجلد وحده** كموقع مستقل:

| المنصّة | الإعداد |
|---|---|
| Netlify / Vercel | موقع ثانٍ من نفس المستودع، `Base directory` = `dashboard` |
| Cloudflare Pages | مشروع ثانٍ، `Build output directory` = `dashboard` |
| أي استضافة | ارفع `dashboard/` فقط |

اربطه بنطاق لا يخمّنه أحد: نطاق آخر تملكه، أو نطاق فرعي غير معلن.

**تأكد أن نشر المتجر لا يشمله.** إن كنت تنشر جذر المستودع فأنت تنشر
اللوحة معه — اجعل جذر نشر المتجر `frontend/` لا الجذر.

## المفاتيح

انسخ `config.example.js` إلى `config.js` وعبّئه:

```js
window.JANEIRO_CONFIG = {
  SUPABASE_URL: "https://xxxx.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGciOi...",
};
```

`config.js` في `.gitignore`. المفتاح `anon` عام بالتصميم — ما يحميك هو
RLS وفحص `is_admin()` في الخادم، لا سرّية المفتاح. **لا تضع
`service_role` هنا إطلاقاً.**

## حساب الأدمن

```sql
-- أنشئ المستخدم من Supabase → Authentication → Add user، ثم:
insert into profiles (id, role, full_name)
values ('USER-UUID-HERE', 'admin', 'اسمك')
on conflict (id) do update set role = 'admin';
```

من ليس له صفّ بدور `admin` يُرفض حتى لو سجّل الدخول بنجاح.

## ما بداخلها

**نظرة عامة** — ما يحتاجك الآن، أرقام اليوم مقارنةً بالأمس، سبعة أيام
كرسم، أكثر المنتجات مبيعاً في 30 يوماً، والإجماليات.

**الطلبات** — طابور بحسب الحالة، وتفاصيل الطلب ببيانات التفعيل وأزرار
نسخ ورابط موقّت للوصل، ونقل الحالة بالأسباب.

**المنتجات** — إضافة وتعديل منتج كامل: البيانات، صورة البوستر وأيقونة
العلامة (كلاهما يُرفع مباشرة إلى bucket `product-media`)، الخطط والأسعار،
المزايا، وحقول التفعيل المطلوبة من العميل. الحفظ يمرّ بدالة واحدة تكتب الجداول الخمسة
في معاملة واحدة، فلا يبقى منتج نصف مبني إن فشل الحفظ.

قواعد يفرضها الخادم لا الشاشة:
- منتج «معروض للبيع» لازم له خطة نشطة واحدة على الأقل
- المعرّف (slug) لا يتغيّر بعد الحفظ — الطلبات والروابط تعتمد عليه
- خطة تحذفها من المحرّر **تُعطَّل ولا تُحذف**: الطلبات السابقة تشير
  إليها، و`daily_deals` يحذف بالتتالي — فحذفها كان سيمحو عرضها
- الأرشفة تخفي المنتج من المتجر وتُبقي طلباته كما هي

### الصور: ماذا ترفع بالضبط

| | البوستر | أيقونة العلامة |
|---|---|---|
| **أين تظهر** | بطاقة المنتج وصفحته | مدار الشعارات في الصفحة الرئيسية، ومصغّرة السلة |
| **الأبعاد** | نسبة 3:4 | **مربّعة 256×256** |
| **الصيغة** | PNG أو JPG أو WebP | **PNG أو WebP بخلفية شفافة** |
| **الحجم** | حتى 5 ميغابايت | حتى 1 ميغابايت |
| **بدونها** | يظهر البوستر البديل المصمَّم | يظهر حرف اسم المنتج |
| **العمود** | `poster_path` | `icon_path` |

قائمة المنتجات تعرض شريطاً بأسماء كل ما ينقصه أيقونة، وعلامة «بلا أيقونة»
على كل صف — حتى تعرف ما يجب رفعه دون فتح المنتجات واحداً واحداً.

**ارفع النسخة الملوّنة أو الداكنة من الشعار، لا النسخة البيضاء.** الدائرة
خلف الأيقونة فاتحة في الثيمين، فالشعار الأبيض يختفي فيها.

**لماذا لا SVG؟** كل صورة في المشروع تُفحص بالـmagic bytes لا بـContent-Type.
الـSVG ملف XML بلا توقيع يُفحص، وإذا خُدم من bucket عام كـ`image/svg+xml`
نفّذ أي سكربت بداخله عند فتح رابطه مباشرة. حدّة الحواف لا تساوي هذا.

**التحقق عند الرفع** يقرأ بايتات الملف نفسها، لا نوعه المُعلن: ملف نصي
أو SVG أُعيدت تسميته `.png` يُرفض. ثم تُفحص الأبعاد بفكّ ترميز الصورة —
غير المربّعة، أو الأصغر من 128px، أو الأكبر من 1024px تُرفض برسالة تقول
السبب والمقاس الفعلي.

### كيف يُحتسب الدخل

الطلب يُحتسب دخلاً من لحظة **تأكيد الدفع**، ويخرج من الحساب إن **استُرجع**.
الطلبات بانتظار الوصل أو تحت المراجعة ليست دخلاً — ليست مالاً بعد،
وعدّها يُجمّل الرقم بلا سبب.
